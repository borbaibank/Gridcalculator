# Optional fallback when request host is unavailable (e.g. local build only).
# SEO metadata, sitemap, and robots use the live request domain automatically on Vercel.
NEXT_PUBLIC_SITE_URL=https://girdcalculator.vercel.app
